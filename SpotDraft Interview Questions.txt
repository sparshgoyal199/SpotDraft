q1 Why did you go with Lang graph for ingestion workflow?

Ans1 - Tomorrow scale, implement check pointer concept, fault tolerance concept, if crash, then start from that same node, instead restart.(External orchestration layer, process supervisor, Kubernetes)

q2 Major challenges in project?
Python executes imported modules recursively

Ans2 - How to initialize the check pointer asynchronously, when we start the Fast API server, there will not be any creation of event loop which responsible for running the co-routines and i am connected to supa base through async code, 

I was not able to initialize the check pointer and supa base client asynchronously, when i was starting my fast API server, because there is not event loop exist then, when we start the server python executes the modules recursively so earlier i have written the async code for the configuration of supa base and check pointer in one of the imported file, so i was getting the error, and these are the variables which i also need to import in my services file for doing the DB operations,

I was not able to initialize the check pointer and supa base client asynchronously, when i was starting my fast API server so first of all it executes the code of all the file we import in main, and due to absence of event loop we cant run those and these are the variables which i also need to import in my services file for doing the DB operations, and i also need to make sure that other services file get the latest value of these check pointer and supa base which are not setup yet in DB python file. So put their initialization code in a function and then i am importing that function in main.py, and then using async context manager which helps me to have event loop and start running my clients, so because of this the variables defined in global env in DB will get be set. and now because these global variables value get updated, so in order to use them in other files, i need to write the import statement in local scope, not in the global scope in the file, because if we write in global scope then there can-not be updated, so we have to write the import statement in local scope because when the function, then it actually get the updated value from DB file.

Three files DB file, main file and other services files, DB file where i write the configuration code regarding the database(supabase) and from that file i share the DB connection to other files, basically DB connection will be in global variable in DB file, now earlier i have written the async code for the database configuration, and as we know when python executes the fast API server it starts running all the imports file first, so because there is no event loop exist so the async code i have written in DB file was giving me the error. and because the configuration cannot be setup, so the files importing the DB connection will also not able to run, so i need to somehow configured the database connection asynchronously in DB file, so what i have done i have created a global variable name as DB Connection set its value = none and defined the function the async function, now i am sending them into main file, because event loop creation will be done after all the files have been imported, and now running that function will be run